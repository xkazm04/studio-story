import SoundStudio from './SoundStudio';

export const metadata = {
  title: 'Sound Studio — Story',
  description: 'Full-screen audio production studio — DAW timeline, stems, sound generation',
};

export default function SoundStudioPage() {
  return <SoundStudio />;
}
