/**
 * Prompt Composer Types
 * Re-exported from the canonical shared types module.
 */

export type {
  PromptDimension,
  PromptOption,
  PromptColumn,
  SelectionState,
} from '../types/ai-writing';

export { MAX_PROMPT_LENGTH } from '../types/ai-writing';
