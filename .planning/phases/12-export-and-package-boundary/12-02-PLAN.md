---
phase: 12-export-and-package-boundary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/export/HTML5BundleGenerator.ts
  - src/lib/export/templates/html5-reader.ts
  - src/lib/export/templates/reader-styles.ts
  - src/lib/export/__tests__/html5-bundle.test.ts
autonomous: true
requirements: [EXPORT-02]

must_haves:
  truths:
    - "HTML5BundleGenerator produces a single self-contained HTML file"
    - "HTML file contains base64-inlined images (no external URLs)"
    - "HTML file contains data URL audio with play buttons per scene"
    - "Reader provides page-by-page scene navigation with arrow keys and click"
    - "Reader theme adapts to the project art style palette"
  artifacts:
    - path: "src/lib/export/HTML5BundleGenerator.ts"
      provides: "Self-contained HTML5 reader export generator"
      exports: ["HTML5BundleGenerator"]
    - path: "src/lib/export/templates/html5-reader.ts"
      provides: "HTML template assembly functions for reader layout"
      exports: ["generateReaderHTML", "generateReaderBody"]
    - path: "src/lib/export/templates/reader-styles.ts"
      provides: "CSS string generation with art-style adaptive theming"
      exports: ["generateReaderCSS"]
    - path: "src/lib/export/__tests__/html5-bundle.test.ts"
      provides: "Unit tests for HTML5 bundle generator"
  key_links:
    - from: "src/lib/export/HTML5BundleGenerator.ts"
      to: "src/lib/export/templates/html5-reader.ts"
      via: "import template functions"
      pattern: "import.*from.*templates/html5-reader"
    - from: "src/lib/export/HTML5BundleGenerator.ts"
      to: "src/lib/export/templates/reader-styles.ts"
      via: "import CSS generator"
      pattern: "import.*generateReaderCSS.*from.*templates/reader-styles"
---

<objective>
Build the HTML5 single-file bundle generator that exports a complete story as a self-contained HTML file with inlined images, audio, adaptive art-style theming, and page-by-page scene navigation.

Purpose: EXPORT-02 -- users can export a shareable HTML5 bundle that works offline by just double-clicking the file.
Output: HTML5BundleGenerator class, HTML/CSS templates, unit tests.
</objective>

<execution_context>
@C:/Users/kazda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-export-and-package-boundary/12-CONTEXT.md
@.planning/phases/12-export-and-package-boundary/12-RESEARCH.md
@src/lib/export/index.ts

<interfaces>
<!-- StoryExportData from types.ts (created in plan 01, but this plan can define its own copy if executing in parallel) -->
```typescript
export interface StoryExportData {
  title: string;
  author: string;
  scenes: StoryExportScene[];
  artStyle?: {
    palette: string[];
    backgroundColor: string;
    accentColor: string;
    fontFamily?: string;
  };
  metadata?: { description?: string; genre?: string; };
}

export interface StoryExportScene {
  id: string;
  name: string;
  content: string;
  imageUrl?: string;
  narrationUrl?: string;
  dialogueLines?: { speaker: string; text: string; audioUrl?: string; }[];
  choices?: { label: string; targetSceneId: string; condition?: Record<string, unknown>; }[];
  isEnding?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create HTML5 reader templates (CSS + HTML)</name>
  <files>src/lib/export/templates/reader-styles.ts, src/lib/export/templates/html5-reader.ts, src/lib/export/__tests__/html5-bundle.test.ts</files>
  <behavior>
    - Test 1: generateReaderCSS with artStyle produces CSS containing the palette backgroundColor and accentColor values
    - Test 2: generateReaderCSS without artStyle produces CSS with sensible dark defaults (slate-950 style)
    - Test 3: generateReaderHTML produces valid HTML with DOCTYPE, charset meta, embedded style block, and script block
    - Test 4: generateReaderBody produces one .scene-page div per scene with scene name heading and content
    - Test 5: Scenes with imageUrl produce img tags with data: src attributes
    - Test 6: Scenes with narrationUrl produce audio elements with play buttons
    - Test 7: Navigation JS handles arrow-key and click scene advancement
  </behavior>
  <action>
    1. Create `src/lib/export/templates/reader-styles.ts`:
       - Export `generateReaderCSS(artStyle?: StoryExportData['artStyle']): string`
       - CSS for full-viewport scene pages (100vh each), centered content, responsive typography
       - If artStyle provided: use backgroundColor for body bg, accentColor for headings/accents, palette[0] for text, fontFamily if set
       - If no artStyle: dark slate theme (bg #0f172a, text #e2e8f0, accent #06b6d4 -- matching --ms- design system)
       - Scene page layout: illustration as hero background/header image, title overlay, prose text below, play button styled as circular icon
       - Navigation dots at bottom of viewport showing scene progress
       - Smooth scroll-snap for page-by-page feel
       - CSS transitions for scene page changes

    2. Create `src/lib/export/templates/html5-reader.ts`:
       - Export `generateReaderBody(scenes: RenderedScene[]): string` where RenderedScene has inlined base64 image and audio data URLs
       - Each scene becomes a `<section class="scene-page">` with: illustration img (if image), `<h2>` scene name, `<div class="scene-content">` with prose, audio play button (if narration)
       - Export `generateReaderJS(sceneCount: number): string` -- vanilla JS for: arrow-key navigation (left/right), click-to-advance, scroll-snap sync, play button audio control, navigation dot highlighting
       - Export `generateReaderHTML(title: string, css: string, body: string, js: string): string` -- assembles complete `<!DOCTYPE html>` document with everything inlined

    3. Write tests covering all behaviors. For HTML output, use string matching (contains checks) not DOM parsing. Test CSS generation with regex for color values.
  </action>
  <verify>
    <automated>npx vitest run src/lib/export/__tests__/html5-bundle.test.ts -x</automated>
  </verify>
  <done>Reader templates produce valid HTML/CSS/JS strings with art-style adaptive theming and scene navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Create HTML5BundleGenerator with asset inlining</name>
  <files>src/lib/export/HTML5BundleGenerator.ts</files>
  <action>
    1. Create `src/lib/export/HTML5BundleGenerator.ts`:
       - Class with async `generate(data: StoryExportData): Promise<ExportResult>` following ExportResult pattern
       - `fetchAndEncodeImage(url: string): Promise<string>` -- fetches image URL, converts to base64 data URL. Handles both http URLs (fetch) and existing data: URLs (pass through)
       - `fetchAndEncodeAudio(url: string): Promise<string>` -- same for audio, produces `data:audio/mpeg;base64,...`
       - For each scene: fetch and encode imageUrl and narrationUrl (and dialogueLines[].audioUrl if present), producing RenderedScene objects
       - Assembly: call generateReaderCSS with artStyle, generateReaderBody with rendered scenes, generateReaderJS with scene count, generateReaderHTML to assemble
       - Return ExportResult with format 'html5', blob of type 'text/html;charset=utf-8', filename from slugify(title) + '.html'
       - Metadata: sceneCount, wordCount (sum of scene content words)

    2. Add test in html5-bundle.test.ts for the full generator:
       - Mock global fetch to return minimal JPEG bytes for images and minimal MP3 bytes for audio
       - Test that generate() returns ExportResult with 'html5' format
       - Test that output HTML contains `data:image/` strings (inlined images)
       - Test that output HTML contains `data:audio/` strings (inlined audio)
       - Test that output contains scene names from input data

    3. Update `src/lib/export/index.ts`: re-export HTML5BundleGenerator, replace 'html5' throw with actual call
  </action>
  <verify>
    <automated>npx vitest run src/lib/export/__tests__/html5-bundle.test.ts -x</automated>
  </verify>
  <done>HTML5BundleGenerator produces a single self-contained HTML file with base64-inlined images and audio, art-style adaptive theme, and page-by-page scene navigation.</done>
</task>

</tasks>

<verification>
npx vitest run src/lib/export/__tests__/html5-bundle.test.ts -x
</verification>

<success_criteria>
- HTML5BundleGenerator.generate() returns a single HTML blob with all assets inlined
- No external URLs in the output HTML (all images/audio are data: URLs)
- Reader CSS adapts to art style palette when provided
- Scene navigation works via embedded JS (arrow keys, click)
- Each scene has a play button for narration audio
</success_criteria>

<output>
After completion, create `.planning/phases/12-export-and-package-boundary/12-02-SUMMARY.md`
</output>
