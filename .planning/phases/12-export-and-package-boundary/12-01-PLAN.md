---
phase: 12-export-and-package-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/export/types.ts
  - src/lib/export/StoryPDFGenerator.ts
  - src/lib/export/index.ts
  - src/lib/export/__tests__/story-pdf.test.ts
autonomous: true
requirements: [EXPORT-01]

must_haves:
  truths:
    - "StoryPDFGenerator produces a valid PDF blob with embedded illustration images"
    - "PDF includes formatted prose text (not just screenplay blocks)"
    - "PDF includes title page with story title and author"
    - "Images are scaled to fit within page content width preserving aspect ratio"
  artifacts:
    - path: "src/lib/export/types.ts"
      provides: "StoryExportData and StoryExportScene interfaces shared by all export generators"
      exports: ["StoryExportData", "StoryExportScene"]
    - path: "src/lib/export/StoryPDFGenerator.ts"
      provides: "PDF generator that embeds illustrations inline between scene prose"
      exports: ["StoryPDFGenerator"]
    - path: "src/lib/export/__tests__/story-pdf.test.ts"
      provides: "Unit tests for StoryPDFGenerator"
  key_links:
    - from: "src/lib/export/StoryPDFGenerator.ts"
      to: "src/lib/export/types.ts"
      via: "import StoryExportData"
      pattern: "import.*StoryExportData.*from.*types"
    - from: "src/lib/export/index.ts"
      to: "src/lib/export/StoryPDFGenerator.ts"
      via: "re-export"
      pattern: "export.*StoryPDFGenerator"
---

<objective>
Create the shared StoryExportData types used by all three export generators, then implement StoryPDFGenerator that produces illustrated story PDFs. Extends the existing ExportFormat type and export barrel.

Purpose: EXPORT-01 -- users can export a story as a formatted PDF with embedded illustrations and styled text.
Output: StoryExportData types, StoryPDFGenerator class, unit tests, updated export barrel.
</objective>

<execution_context>
@C:/Users/kazda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-export-and-package-boundary/12-CONTEXT.md
@.planning/phases/12-export-and-package-boundary/12-RESEARCH.md
@src/lib/export/index.ts
@src/lib/export/PDFGenerator.ts

<interfaces>
<!-- Existing ExportResult pattern that new generators must follow -->
From src/lib/export/index.ts:
```typescript
export type ExportFormat = 'pdf' | 'epub' | 'fountain' | 'rtf' | 'docx' | 'txt';

export interface ExportResult {
  blob: Blob;
  filename: string;
  format: ExportFormat;
  metadata: {
    pageCount?: number;
    chapterCount?: number;
    sceneCount?: number;
    wordCount?: number;
  };
}

export function downloadExport(result: ExportResult): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared StoryExportData types and StoryPDFGenerator</name>
  <files>src/lib/export/types.ts, src/lib/export/StoryPDFGenerator.ts, src/lib/export/__tests__/story-pdf.test.ts</files>
  <behavior>
    - Test 1: StoryPDFGenerator.generate() returns ExportResult with format 'story-pdf', non-empty blob, and .pdf filename
    - Test 2: PDF blob starts with %PDF magic bytes
    - Test 3: Scenes with imageUrl produce PDF containing image XObjects (search for /Subtype /Image in PDF stream)
    - Test 4: Scenes without imageUrl produce valid PDF without image errors
    - Test 5: Title page includes story title and author strings in the PDF content
    - Test 6: Multiple scenes produce multi-page PDF with scene count in metadata
  </behavior>
  <action>
    1. Create `src/lib/export/types.ts` with StoryExportData and StoryExportScene interfaces as specified in 12-RESEARCH.md (title, author, scenes array with content/imageUrl/narrationUrl/dialogueLines/choices/isEnding, artStyle optional, metadata optional). Also export a helper `slugify(title: string): string` for filename generation.

    2. Create `src/lib/export/StoryPDFGenerator.ts`:
       - Class with async `generate(data: StoryExportData): Promise<ExportResult>` method
       - Builds on the same hand-rolled PDF binary approach as existing PDFGenerator (do NOT use a library)
       - For each scene: render scene name as heading, render content as wrapped prose paragraphs, then embed illustration image if imageUrl exists
       - Image embedding: fetch image URL, get raw bytes, determine dimensions, scale to fit within 432pt (6 inch) content width while preserving aspect ratio, embed as PDF image XObject with DCTDecode (JPEG) or FlateDecode (PNG)
       - Title page: centered title + author + optional genre/description
       - Use SCREENPLAY_FORMAT-style constants for margins (72pt margins, 468pt page width)
       - For images, accept both remote URLs (fetch) and data URLs (decode directly). In test environment, mock fetch.
       - Return ExportResult with format 'story-pdf' (add to ExportFormat union)

    3. Write tests in `src/lib/export/__tests__/story-pdf.test.ts` per behavior spec above. Mock fetch for image URLs returning a minimal valid JPEG (FF D8 FF header + minimal data). Use StoryExportData fixtures with 2-3 scenes.
  </action>
  <verify>
    <automated>npx vitest run src/lib/export/__tests__/story-pdf.test.ts -x</automated>
  </verify>
  <done>StoryPDFGenerator produces valid PDF blobs with embedded images and formatted prose text. StoryExportData types exported for use by HTML5 and VN generators.</done>
</task>

<task type="auto">
  <name>Task 2: Update export barrel and ExportFormat type</name>
  <files>src/lib/export/index.ts</files>
  <action>
    1. In `src/lib/export/index.ts`:
       - Update ExportFormat type to include 'story-pdf', 'html5', 'visual-novel' (all three new formats, even though html5 and visual-novel generators come in later plans -- the type should be complete)
       - Re-export everything from `./types` (StoryExportData, StoryExportScene, slugify)
       - Re-export StoryPDFGenerator from `./StoryPDFGenerator`
       - Add 'story-pdf' case to the exportScript switch (call StoryPDFGenerator, converting ScriptData to StoryExportData with basic mapping)
       - Leave 'html5' and 'visual-novel' cases as `throw new Error('Not yet implemented')` placeholders
  </action>
  <verify>
    <automated>npx vitest run src/lib/export/__tests__/story-pdf.test.ts -x</automated>
  </verify>
  <done>ExportFormat includes all new formats. StoryPDFGenerator and shared types re-exported from barrel. Existing export tests still pass.</done>
</task>

</tasks>

<verification>
npx vitest run src/lib/export/__tests__/ -x
</verification>

<success_criteria>
- StoryPDFGenerator.generate() returns a valid PDF blob with embedded scene illustrations
- StoryExportData types are defined and exported for downstream generators
- ExportFormat union includes 'story-pdf', 'html5', 'visual-novel'
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-export-and-package-boundary/12-01-SUMMARY.md`
</output>
