---
phase: 07-story-data-and-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/types/Project.ts
  - src/lib/supabase/database.types.ts
  - src/mcp-server/tools/projects.ts
  - src/mcp-server/tools/characters.ts
  - src/workspace/types.ts
  - src/workspace/engine/panelRegistry.ts
  - src/manifest/panelManifests.ts
  - src/workspace/panels/character/RelationshipMapPanel.tsx
  - src/workspace/hooks/useCLIDataSync.ts
autonomous: true
requirements:
  - STORY-01
  - STORY-02
  - STORY-03

must_haves:
  truths:
    - "Project type includes premise, genre, and setting fields"
    - "LLM can create and update projects with premise/genre/setting via MCP tools"
    - "LLM can create relationships between characters via MCP tool"
    - "relationship-map is a registered workspace panel type"
    - "Query invalidation covers relationships and project mutations"
  artifacts:
    - path: "src/app/types/Project.ts"
      provides: "Project interface with premise, genre, setting"
      contains: "premise"
    - path: "src/mcp-server/tools/projects.ts"
      provides: "create_project and update_project MCP tools"
      contains: "update_project"
    - path: "src/workspace/engine/panelRegistry.ts"
      provides: "relationship-map panel registration"
      contains: "relationship-map"
    - path: "src/workspace/panels/character/RelationshipMapPanel.tsx"
      provides: "Relationship map panel wrapper"
    - path: "src/manifest/panelManifests.ts"
      provides: "relationship-map manifest for LLM composition"
      contains: "relationship-map"
  key_links:
    - from: "src/mcp-server/tools/projects.ts"
      to: "src/mcp-server/db.ts"
      via: "dbInsert/dbUpdate for projects"
      pattern: "dbInsert.*projects|dbUpdate.*projects"
    - from: "src/workspace/engine/panelRegistry.ts"
      to: "src/workspace/panels/character/RelationshipMapPanel.tsx"
      via: "lazy import in registry"
      pattern: "import.*RelationshipMapPanel"
    - from: "src/workspace/hooks/useCLIDataSync.ts"
      to: "@tanstack/react-query"
      via: "invalidation map for relationships and projects"
      pattern: "create_relationship.*relationships|update_project.*projects"
---

<objective>
Wire story data foundations: extend the Project schema for conversation-driven creation, add missing MCP tools (create_project, update_project, create_relationship), register a relationship-map panel type in the Jinn workspace, and ensure query invalidation covers all story mutations.

Purpose: This is the data foundation that Plans 02-04 build on. Without project schema fields, the LLM cannot store premise/genre/setting. Without the relationship-map panel type, the LLM cannot compose it. Without create_relationship MCP tool, characters can't be connected.

Output: Updated Project type, new MCP tools, relationship-map panel registered in workspace with manifest, expanded query invalidation map.
</objective>

<execution_context>
@C:/Users/kazda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-story-data-and-authoring/07-CONTEXT.md
@.planning/phases/07-story-data-and-authoring/07-RESEARCH.md

@src/app/types/Project.ts
@src/lib/supabase/database.types.ts
@src/mcp-server/tools/projects.ts
@src/mcp-server/tools/characters.ts
@src/mcp-server/db.ts
@src/workspace/types.ts
@src/workspace/engine/panelRegistry.ts
@src/manifest/panelManifests.ts
@src/manifest/types.ts
@src/workspace/hooks/useCLIDataSync.ts
@src/app/features/relationships/RelationshipMap.tsx
@src/app/features/relationships/types/index.ts

<interfaces>
<!-- Key types the executor needs -->

From src/app/types/Project.ts (CURRENT -- needs extension):
```typescript
export interface Project {
  id: string;
  name: string;
  description?: string;
  type?: string;
  word_count?: number;
  coverImageUrl?: string | null;
  // ... other fields
}
```

From src/mcp-server/db.ts (used by all MCP tools):
```typescript
export function dbInsert(table: string, row: Record<string, unknown>): Promise<DbResult>;
export function dbUpdate(table: string, id: string, updates: Record<string, unknown>): Promise<DbResult>;
export function dbSelect(table: string, options?: DbSelectOptions): Promise<DbResult>;
export function dbSelectOne(table: string, id: string): Promise<DbResult>;
```

From src/workspace/types.ts (WorkspacePanelType union -- needs 'relationship-map' added):
```typescript
export type WorkspacePanelType =
  | 'scene-editor' | 'character-cards' | 'story-map' | ... | 'storyboard';
```

From src/workspace/engine/panelRegistry.ts:
```typescript
export interface PanelRegistryEntry {
  type: WorkspacePanelType;
  label: string;
  icon: LucideIcon;
  importFn: () => Promise<{ default: React.ComponentType<Record<string, unknown>> }>;
  defaultRole: PanelRole;
  sizeClass: PanelSizeClass;
  minWidth?: number;
  domains: SkillDomain[];
  complexity: PanelComplexity;
  manifest?: PanelManifest;
}
```

From src/workspace/hooks/useCLIDataSync.ts (CURRENT -- needs expansion):
```typescript
const TOOL_INVALIDATION_MAP: Record<string, string[]> = {
  create_character: ['characters'],
  update_character: ['characters'],
  create_act: ['acts'],
  create_scene: ['scenes'],
  // ... missing: create_project, update_project, create_relationship
};
```

Existing MCP tool names (confirmed): get_project, list_projects, create_character, update_character, create_faction, update_faction, create_act, create_beat, update_beat, create_scene, update_scene, create_trait, update_trait, list_relationships, compose_workspace
Missing: create_project, update_project, create_relationship
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Project schema and database types</name>
  <files>
    src/app/types/Project.ts,
    src/lib/supabase/database.types.ts
  </files>
  <action>
1. **Add project columns via Supabase migration.** Run SQL against Supabase to add `premise TEXT`, `genre TEXT`, `setting TEXT` columns to the `projects` table. Use the same pattern as the migration from 2026-02-21 (run via pg module against Supabase pooler URL, or via Supabase dashboard SQL editor). If Supabase is not available, create the migration SQL in a comment at the top of the Project type file so it can be run manually.

2. **Update `src/app/types/Project.ts`:** Add `premise?: string`, `genre?: string`, `setting?: string` fields to the Project interface.

3. **Update `src/lib/supabase/database.types.ts`:** Add `premise`, `genre`, `setting` to the projects table Row/Insert/Update types (all nullable strings).
  </action>
  <verify>
    <automated>cd C:/Users/kazda/kiro/studio-story && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>Project type and database types include premise, genre, setting fields. Migration SQL applied or documented for manual execution.</done>
</task>

<task type="auto">
  <name>Task 2: Add missing MCP tools and query invalidation</name>
  <files>
    src/mcp-server/tools/projects.ts,
    src/mcp-server/tools/characters.ts,
    src/workspace/hooks/useCLIDataSync.ts
  </files>
  <action>
1. **Add `create_project` and `update_project` to `src/mcp-server/tools/projects.ts`:**
   - `create_project`: Required: `name`. Optional: `description`, `premise`, `genre`, `setting`, `type`. Uses `dbInsert('projects', row)`. The `user_id` should be set to the config's userId or a default UUID (check how other tools handle user_id -- the Supabase service role bypasses RLS so it may not be needed, but the column has NOT NULL constraint so check the schema).
   - `update_project`: Required: `projectId`. Optional: `name`, `description`, `premise`, `genre`, `setting`, `type`. Uses `dbUpdate('projects', id, updates)`.
   Follow the exact pattern of existing tools in the file (textContent/errorContent helpers, Zod schemas, config.projectId fallback).

2. **Add `create_relationship` to `src/mcp-server/tools/characters.ts`:**
   - Required: `sourceId` (character or faction ID), `targetId` (character or faction ID), `type` (relationship type string).
   - Optional: `description`, `strength` (number 1-10).
   - Uses `dbInsert('relationships', row)` with `project_id` from config.
   - Note: The `list_relationships` tool already exists but create is missing. Follow the same pattern.

3. **Expand `TOOL_INVALIDATION_MAP` in `src/workspace/hooks/useCLIDataSync.ts`:** Add entries for:
   - `create_project: ['projects']`
   - `update_project: ['projects']`
   - `create_relationship: ['relationships', 'characters']` (characters too because relationship changes affect character detail panels)
  </action>
  <verify>
    <automated>cd C:/Users/kazda/kiro/studio-story && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>MCP tools create_project, update_project, create_relationship exist and follow established patterns. TOOL_INVALIDATION_MAP covers all new mutation tools.</done>
</task>

<task type="auto">
  <name>Task 3: Register relationship-map panel type with manifest</name>
  <files>
    src/workspace/types.ts,
    src/workspace/engine/panelRegistry.ts,
    src/manifest/panelManifests.ts,
    src/workspace/panels/character/RelationshipMapPanel.tsx
  </files>
  <action>
1. **Add `'relationship-map'` to the `WorkspacePanelType` union** in `src/workspace/types.ts`. Insert it after `'cast-sidebar'` in the character-related group.

2. **Create `src/workspace/panels/character/RelationshipMapPanel.tsx`:** A thin panel wrapper that imports the existing `RelationshipMap` component from `src/app/features/relationships/RelationshipMap.tsx` and wraps it in `PanelFrame`. Accept `density?: PanelDensity`, `projectId?: string`, `onClose?: () => void` props. Resolve `projectId` from props or `useProjectStore().selectedProject?.id`. For micro density, show just the panel badge with a network icon and "N relationships" count. For compact, show the map without the filter bar. For full, show the complete map with filters. Import the `GitBranch` or `Network` icon from lucide-react. Export as default for lazy loading. Follow the exact pattern of existing panels like `StoryMapPanel.tsx`.

3. **Register in `src/workspace/engine/panelRegistry.ts`:** Add a `'relationship-map'` entry to `PANEL_REGISTRY`:
   - type: `'relationship-map'`
   - label: `'Relationship Map'`
   - icon: Import `Network` from lucide-react (add to imports)
   - importFn: `() => import('../panels/character/RelationshipMapPanel')`
   - defaultRole: `'primary'`
   - sizeClass: `'wide'`
   - minWidth: `400`
   - domains: `['character']`
   - complexity: `'high'`
   - manifest: `getManifest('relationship-map')`

4. **Add manifest in `src/manifest/panelManifests.ts`:** Add a manifest entry for `relationship-map`:
   - type: `'relationship-map'`
   - label: `'Relationship Map'`
   - description: `'Interactive visual graph of character relationships and faction dynamics using ReactFlow. Shows character nodes grouped by faction with colored edges for relationship types. Read-only -- relationships are created via chat.'`
   - capabilities: Visualize character connections, Show faction clusters, Filter by relationship type, Pan and zoom navigation, Click to focus character
   - domains: `['character']`
   - inputSchema: optional `projectId`
   - useCases: "User wants to see character connections", "User asks about faction dynamics", "User wants to understand relationship networks"
   - layout: defaultRole `'primary'`, sizeClass `'wide'`, minWidth 400
   - complexity: `'high'`
   - icon: `'Network'`
   - suggestedCompanions: `['character-detail', 'character-cards']`
   - densityModes: full (400x300, full interactive map with filters), compact (250x180, map without filter bar), micro (100x48, relationship count badge)
   - dataSliceExamples: `[{ scenario: 'Show project relationships', dataSlice: {} }, { scenario: 'Highlight specific character', dataSlice: { entityId: 'char-123' } }]`
  </action>
  <verify>
    <automated>cd C:/Users/kazda/kiro/studio-story && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>relationship-map is a valid WorkspacePanelType, registered in PANEL_REGISTRY with lazy import, has a manifest entry the LLM can read for composition, and the panel component wraps the existing RelationshipMap with PanelFrame and density support.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors from new panel type, extended Project interface, or new MCP tools)
- `relationship-map` appears in WorkspacePanelType union
- PANEL_REGISTRY has a `relationship-map` entry
- panelManifests array includes relationship-map manifest
- TOOL_INVALIDATION_MAP covers create_project, update_project, create_relationship
</verification>

<success_criteria>
- Project type has premise, genre, setting fields
- MCP server has create_project, update_project, create_relationship tools
- relationship-map is a first-class workspace panel type with manifest
- Query invalidation covers all new mutation tools
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-story-data-and-authoring/07-01-SUMMARY.md`
</output>
