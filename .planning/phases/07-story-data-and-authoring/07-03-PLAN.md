---
phase: 07-story-data-and-authoring
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/app/features/story/sub_SceneEditor/extensions/screenplayNodes.ts
  - src/app/features/story/sub_SceneEditor/extensions/screenplayKeymap.ts
  - src/app/features/story/sub_SceneEditor/components/ScreenplayToolbar.tsx
  - src/workspace/panels/scene/SceneEditorPanel.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - STORY-04

must_haves:
  truths:
    - "User can insert scene headings (INT/EXT) via toolbar or keyboard shortcut"
    - "User can write dialogue with character name prefix in screenplay style"
    - "User can toggle distraction-free mode with F11 or Cmd+Shift+F"
    - "Formatting toolbar is fixed at top with both prose and screenplay tools"
    - "Screenplay elements render with distinct visual styling"
  artifacts:
    - path: "src/app/features/story/sub_SceneEditor/extensions/screenplayNodes.ts"
      provides: "TipTap custom nodes for screenplay elements"
      exports: ["SceneHeading", "ActionLine", "CharacterCue", "Dialogue", "Parenthetical"]
    - path: "src/app/features/story/sub_SceneEditor/extensions/screenplayKeymap.ts"
      provides: "Keyboard shortcuts for screenplay workflow"
      exports: ["ScreenplayKeymap"]
    - path: "src/app/features/story/sub_SceneEditor/components/ScreenplayToolbar.tsx"
      provides: "Combined prose + screenplay formatting toolbar"
      exports: ["ScreenplayToolbar"]
  key_links:
    - from: "src/workspace/panels/scene/SceneEditorPanel.tsx"
      to: "src/app/features/story/sub_SceneEditor/extensions/screenplayNodes.ts"
      via: "TipTap useEditor extensions array"
      pattern: "SceneHeading|ActionLine|CharacterCue|Dialogue|Parenthetical"
    - from: "src/workspace/panels/scene/SceneEditorPanel.tsx"
      to: "src/app/features/story/sub_SceneEditor/components/ScreenplayToolbar.tsx"
      via: "toolbar rendered at top of editor"
      pattern: "ScreenplayToolbar"
---

<objective>
Add screenplay-specific TipTap extensions to the scene editor: custom node types for scene heading, action, character cue, dialogue, and parenthetical. Add a combined prose+screenplay toolbar and distraction-free mode.

Purpose: STORY-04 requires rich text editing with screenplay markup. The existing SceneEditorPanel has a block-based editor but uses custom markdown-like @markers rather than TipTap structured nodes. This plan adds proper TipTap screenplay extensions that can be composed alongside prose formatting, making the editor feel like a Scrivener/Final Draft hybrid per CONTEXT.md.

Output: 5 TipTap custom node extensions, a ScreenplayKeymap extension, a combined toolbar, and distraction-free toggle on the SceneEditorPanel.
</objective>

<execution_context>
@C:/Users/kazda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-story-data-and-authoring/07-CONTEXT.md
@.planning/phases/07-story-data-and-authoring/07-RESEARCH.md

@src/app/components/UI/RichTextEditor.tsx
@src/workspace/panels/scene/SceneEditorPanel.tsx

<interfaces>
<!-- Key types the executor needs -->

From @tiptap/core (installed at ^3.10.1):
```typescript
import { Node, mergeAttributes } from '@tiptap/core';
import { Extension } from '@tiptap/core';

// Node.create() pattern for custom block nodes:
const MyNode = Node.create({
  name: 'myNode',
  group: 'block',
  content: 'text*',
  defining: true,
  addAttributes() { return { ... }; },
  parseHTML() { return [{ tag: 'div[data-type="my-node"]' }]; },
  renderHTML({ HTMLAttributes }) {
    return ['div', mergeAttributes(HTMLAttributes, { 'data-type': 'my-node' }), 0];
  },
  addKeyboardShortcuts() { return { ... }; },
});
```

From @tiptap/react:
```typescript
import { useEditor, EditorContent, Editor } from '@tiptap/react';
```

Existing SceneEditorPanel uses a CUSTOM block editor (NOT TipTap):
- Custom BlockType = 'scene' | 'action' | 'dialogue' | 'direction' | 'content' | 'beat'
- Uses @markers in markdown-like format
- Has its own parseBlocks/serializeBlocks functions
The plan should ADD a TipTap-based editor mode alongside or replacing the block editor.

CONTEXT.md Screenplay Decisions:
- Scene heading (INT/EXT), action lines, character cue, dialogue, parenthetical
- Character name prefix for dialogue: "ELENA: I won't let you leave."
- Fixed top toolbar with prose + screenplay sections
- Distraction-free toggle: F11 or Cmd+Shift+F expands editor full workspace
- Prose formatting: bold, italic, headings H1-H3, blockquote, horizontal rule
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TipTap screenplay extensions and keymap</name>
  <files>
    src/app/features/story/sub_SceneEditor/extensions/screenplayNodes.ts,
    src/app/features/story/sub_SceneEditor/extensions/screenplayKeymap.ts
  </files>
  <action>
1. **Create `src/app/features/story/sub_SceneEditor/extensions/screenplayNodes.ts`:**

   Define 5 TipTap custom Node extensions using `Node.create()`:

   **SceneHeading:**
   - name: `'sceneHeading'`
   - group: `'block'`, content: `'text*'`, defining: true
   - Attributes: `locationType` (default `'INT'`, values `'INT'` | `'EXT'` | `'INT/EXT'`)
   - parseHTML: `[{ tag: 'div[data-type="scene-heading"]' }]`
   - renderHTML: `['div', mergeAttributes(HTMLAttributes, { 'data-type': 'scene-heading', class: 'screenplay-scene-heading' }), 0]`
   - Note: The CSS class name is defined here in renderHTML; the actual CSS styles will be added in Task 2 via globals.css.

   **ActionLine:**
   - name: `'actionLine'`
   - group: `'block'`, content: `'text*'`, defining: true
   - parseHTML: `[{ tag: 'div[data-type="action-line"]' }]`
   - renderHTML: class `'screenplay-action'`

   **CharacterCue:**
   - name: `'characterCue'`
   - group: `'block'`, content: `'text*'`, defining: true
   - parseHTML: `[{ tag: 'div[data-type="character-cue"]' }]`
   - renderHTML: class `'screenplay-character-cue'`

   **Dialogue:**
   - name: `'dialogue'`
   - group: `'block'`, content: `'text*'`, defining: true
   - parseHTML: `[{ tag: 'div[data-type="dialogue"]' }]`
   - renderHTML: class `'screenplay-dialogue'`

   **Parenthetical:**
   - name: `'parenthetical'`
   - group: `'block'`, content: `'text*'`, defining: true
   - parseHTML: `[{ tag: 'div[data-type="parenthetical"]' }]`
   - renderHTML: class `'screenplay-parenthetical'`

   Export all 5 as named exports. Do NOT add CSS to globals.css in this task -- that is Task 2's responsibility.

2. **Create `src/app/features/story/sub_SceneEditor/extensions/screenplayKeymap.ts`:**

   Create a TipTap `Extension` (not Node) using `Extension.create()`:
   - name: `'screenplayKeymap'`
   - `addKeyboardShortcuts()` returns:
     - `'Mod-Shift-h'`: Set current block to SceneHeading
     - `'Mod-Shift-a'`: Set current block to ActionLine
     - `'Mod-Shift-c'`: Set current block to CharacterCue
     - `'Mod-Shift-d'`: Set current block to Dialogue
     - `'Mod-Shift-p'`: Set current block to Parenthetical
   - Each shortcut calls `this.editor.commands.setNode('nodeName')` to transform the current block.

   Export as named export `ScreenplayKeymap`.
  </action>
  <verify>
    <automated>cd C:/Users/kazda/kiro/studio-story && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>5 TipTap Node extensions (SceneHeading, ActionLine, CharacterCue, Dialogue, Parenthetical) created with parse/render HTML and CSS class names. ScreenplayKeymap extension provides keyboard shortcuts for block type switching.</done>
</task>

<task type="auto">
  <name>Task 2: Add screenplay toolbar, distraction-free mode, and all screenplay CSS</name>
  <files>
    src/app/features/story/sub_SceneEditor/components/ScreenplayToolbar.tsx,
    src/workspace/panels/scene/SceneEditorPanel.tsx,
    src/app/globals.css
  </files>
  <action>
1. **Create `src/app/features/story/sub_SceneEditor/components/ScreenplayToolbar.tsx`:**

   A fixed toolbar component accepting `editor: Editor | null` prop. Two sections separated by a visual divider:

   **Prose section (left):**
   - Bold (Ctrl+B), Italic (Ctrl+I) -- toggle marks
   - H1, H2, H3 -- set heading level
   - Blockquote, Horizontal Rule
   - Bullet list, Ordered list

   **Screenplay section (right):**
   - Scene Heading (Ctrl+Shift+H) -- icon: `Film`
   - Action (Ctrl+Shift+A) -- icon: `AlignLeft`
   - Character (Ctrl+Shift+C) -- icon: `User`
   - Dialogue (Ctrl+Shift+D) -- icon: `MessageCircle`
   - Parenthetical (Ctrl+Shift+P) -- icon: `Parentheses` or `Minus`

   Each button:
   - Shows active state when the cursor is in that node type (use `editor.isActive('sceneHeading')` etc.)
   - Uses the existing button pattern from RichTextEditor.tsx: `clsx('p-2 rounded hover:bg-slate-700 transition-colors', isActive ? 'bg-slate-700 text-cyan-400' : 'text-slate-400')`
   - Has a `title` attribute with the keyboard shortcut hint
   - Icon size 16px (lucide-react icons)

   Layout: `flex items-center gap-1 p-2 border-b border-slate-800 bg-slate-900/50 sticky top-0 z-10`

   Export as named export.

2. **Integrate into `src/workspace/panels/scene/SceneEditorPanel.tsx`:**

   The existing SceneEditorPanel has a custom block editor. The approach is to add a **TipTap mode** alongside the existing block editor, toggled by a small mode switch in the panel header. This avoids breaking the existing editor while adding the new capability.

   Add:
   - A `mode` state: `'blocks' | 'tiptap'` (default `'tiptap'` for new users; `'blocks'` for backward compat if scene has @markers).
   - When `mode === 'tiptap'`:
     - Create a TipTap editor instance with `useEditor` hook, configured with `StarterKit` (already installed) PLUS the 5 screenplay extensions + ScreenplayKeymap.
     - Set `immediatelyRender: false` to avoid SSR hydration mismatch.
     - Render `<ScreenplayToolbar editor={editor} />` at top.
     - Render `<EditorContent editor={editor} />` below.
     - On content change, call the existing save function with `editor.getHTML()`.
     - Load initial content from the scene's `description` or `content` field.
   - When `mode === 'blocks'`: Render the existing block editor (unchanged).
   - Auto-detect mode: If the scene content starts with `@scene` or `@action` (old @marker format), default to `'blocks'`. Otherwise, default to `'tiptap'`.

   **Distraction-free mode:**
   - Add a `distractionFree` boolean state.
   - Register a keyboard event listener for `F11` and `Cmd+Shift+F` (or `Ctrl+Shift+F` on non-Mac).
   - When active: the SceneEditorPanel renders with `className="fixed inset-0 z-[8500] bg-slate-950"` -- covering the entire viewport above all other panels but below the advisor overlay (z-9000).
   - When inactive: normal panel rendering.
   - Show a small toggle button in the toolbar (Maximize2 / Minimize2 icon from lucide-react) and a tooltip "Distraction-free mode (F11)".
   - In distraction-free mode, hide the PanelFrame header and show only the toolbar + editor.

3. **Add ALL screenplay CSS to `src/app/globals.css`:**
   This is the single location for all screenplay-related CSS. Add a clearly marked section `/* --- Screenplay Node Styles --- */` with styles for:
   - `.screenplay-scene-heading` -- uppercase, bold, 14px, letter-spacing 1px, color cyan-400, border-bottom 1px slate-700, padding-bottom 4px, margin-top 24px
   - `.screenplay-action` -- normal text, slate-200 color, margin 8px 0
   - `.screenplay-character-cue` -- uppercase, bold, text-center, margin-top 16px, margin-left 25%, color slate-100
   - `.screenplay-dialogue` -- margin-left 15%, margin-right 15%, color slate-200
   - `.screenplay-parenthetical` -- italic, margin-left 20%, color slate-400, before/after content `'('` / `')'` (purely visual, via CSS ::before/::after)
   - `.ProseMirror:focus { outline: none; }` (verify if already exists, add only if missing)
   - `.ProseMirror` min-height for the editor area
   Use the `--ms-*` custom properties where appropriate for theme consistency. The styles must work within the TipTap `.ProseMirror` container.
  </action>
  <verify>
    <automated>cd C:/Users/kazda/kiro/studio-story && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>ScreenplayToolbar renders prose + screenplay sections with active states. SceneEditorPanel supports TipTap mode with screenplay extensions. Distraction-free mode toggles via F11/Cmd+Shift+F. All screenplay CSS classes defined in globals.css in a single marked section.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- SceneEditorPanel renders TipTap editor with screenplay extensions when mode is 'tiptap'
- ScreenplayToolbar shows both prose and screenplay formatting buttons
- Keyboard shortcuts (Mod+Shift+H/A/C/D/P) transform block types
- F11 toggles distraction-free mode
- Screenplay CSS classes applied in globals.css (single location, no duplication)
- Existing block editor mode preserved for backward compatibility
</verification>

<success_criteria>
- All 5 screenplay node types (scene heading, action, character cue, dialogue, parenthetical) are available in the editor
- Toolbar has two sections: prose formatting (left) and screenplay elements (right)
- Keyboard shortcuts transform current block to screenplay types
- Distraction-free mode covers the viewport on F11 or Cmd+Shift+F
- Screenplay elements render with distinct visual styling (centered dialogue, uppercase headings, etc.)
- Existing scenes with @marker format still load in blocks mode
</success_criteria>

<output>
After completion, create `.planning/phases/07-story-data-and-authoring/07-03-SUMMARY.md`
</output>
