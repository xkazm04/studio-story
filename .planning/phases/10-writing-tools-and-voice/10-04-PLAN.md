---
phase: 10-writing-tools-and-voice
plan: 04
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - src/app/features/voice/hooks/useScreenplayParser.ts
  - src/app/features/voice/hooks/useVoiceCloning.ts
  - src/app/features/voice/hooks/useNarrationBatch.ts
  - src/app/features/voice/hooks/useNarrationExport.ts
  - src/app/features/voice/components/VoiceAssignmentChecklist.tsx
  - src/app/features/voice/components/__tests__/VoiceAssignmentChecklist.test.tsx
  - src/app/features/voice/hooks/__tests__/useNarrationBatch.test.ts
  - src/app/features/voice/components/NarrationPipeline.tsx
  - src/mcp-server/tools/voice.ts
  - src/workspace/config/workflowHints.ts
autonomous: true
requirements: [VOICE-01, VOICE-02]
user_setup:
  - service: elevenlabs
    why: "TTS voice generation and voice cloning"
    env_vars:
      - name: ELEVENLABS_API_KEY
        source: "ElevenLabs Dashboard -> Profile + API key"

must_haves:
  truths:
    - "User can trigger audio generation for a scene and hear character-appropriate voices"
    - "Screenplay format is auto-detected and each character cue maps to that character's assigned voice"
    - "Action lines and non-dialogue text use the dedicated narrator voice"
    - "User sees a pre-flight checklist of characters needing voice assignments before generation"
    - "User can generate 2-3 takes per segment and audition them"
    - "User can export scene narration as a single mixed MP3"
    - "Voice cloning from direct audio upload is available (YouTube extraction deferred -- no extraction route exists)"
  artifacts:
    - path: "src/app/features/voice/hooks/useScreenplayParser.ts"
      provides: "React hook wrapping screenplay parser for UI"
      exports: ["useScreenplayParser"]
    - path: "src/app/features/voice/hooks/useVoiceCloning.ts"
      provides: "Audio upload -> ElevenLabs clone orchestration"
      exports: ["useVoiceCloning"]
    - path: "src/app/features/voice/components/VoiceAssignmentChecklist.tsx"
      provides: "Pre-flight checklist for voice assignments before generation"
      exports: ["VoiceAssignmentChecklist"]
    - path: "src/mcp-server/tools/voice.ts"
      provides: "MCP tools for voice operations"
      exports: ["voiceTools"]
  key_links:
    - from: "src/app/features/voice/hooks/useNarrationBatch.ts"
      to: "/api/ai/audio/tts"
      via: "fetch in generation loop"
      pattern: "api/ai/audio/tts"
    - from: "src/app/features/voice/hooks/useNarrationExport.ts"
      to: "src/app/features/voice/lib/audioStitcher.ts"
      via: "import stitchAudioSegments"
      pattern: "stitchAudioSegments"
    - from: "src/app/features/voice/components/NarrationPipeline.tsx"
      to: "src/app/features/voice/hooks/useScreenplayParser.ts"
      via: "hook call for screenplay parsing"
      pattern: "useScreenplayParser"
---

<objective>
Wire the voice/narration pipeline UI: connect NarrationPipeline to real TTS routes, add screenplay parsing hook, voice assignment checklist, voice cloning hook (direct audio upload -- YouTube extraction deferred), narration export with MP3 stitching, and MCP tools + workflow hints for LLM composition.

Purpose: This delivers the complete multi-voice audio drama workflow: parse screenplay -> check voice assignments -> generate takes -> audition -> export MP3. Also adds LLM composition awareness for voice workflows.
Output: Working narration pipeline with real TTS, voice cloning (audio upload), MP3 export, MCP voice tools.
</objective>

<execution_context>
@C:/Users/kazda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-writing-tools-and-voice/10-CONTEXT.md
@.planning/phases/10-writing-tools-and-voice/10-RESEARCH.md
@.planning/phases/10-writing-tools-and-voice/10-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 -->
From src/app/features/voice/lib/screenplayParser.ts:
  export function parseScreenplayToScriptLines(tiptapJson, characterVoiceMap, narratorVoiceId): ScriptLine[]
  export function detectUnassignedVoices(tiptapJson, characterVoiceMap): string[]
  export function extractText(node): string

From src/app/features/voice/lib/audioStitcher.ts:
  export function stitchAudioSegments(segments): ArrayBuffer
  export function computeGaps(lines): number[]
  export const GAP_SAME_CHARACTER, GAP_CHAR_CHANGE

API routes from Plan 02:
  POST /api/ai/audio/tts -> { audioUrl, duration }
  POST /api/ai/audio/clone-voice -> { voice_id, name }
  GET /api/ai/audio/voices -> { voices: [...] }

<!-- Existing voice components -->
NarrationPipeline: main pipeline component (needs wiring to real routes)
TakesGallery/TakesModal: existing take audition UI
VoiceConfiguration: existing voice settings UI
ScriptEditor: existing script line editor
useNarrationBatch: existing hook (calls /api/ai/audio/tts which now exists from Plan 02)
useNarrationExport: existing hook (needs audioStitcher wiring)

<!-- MCP pattern from Phase 9 -->
MCP tools follow trio pattern: generate/check/save
Tool declarations in src/mcp-server/tools/*.ts
workflowHints in src/workspace/config/workflowHints.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Voice hooks (4 hooks)</name>
  <files>src/app/features/voice/hooks/useScreenplayParser.ts, src/app/features/voice/hooks/useVoiceCloning.ts, src/app/features/voice/hooks/useNarrationBatch.ts, src/app/features/voice/hooks/useNarrationExport.ts, src/app/features/voice/hooks/__tests__/useNarrationBatch.test.ts</files>
  <behavior>
    - useNarrationBatch.generateAll calls /api/ai/audio/tts for each script line with correct voice_id
    - useNarrationBatch supports takesCount option to generate multiple takes per line
    - useNarrationBatch tracks progress as { current, total, currentCharacter }
    - useNarrationExport.exportMp3 calls computeGaps then stitchAudioSegments
  </behavior>
  <action>
Create `src/app/features/voice/hooks/useScreenplayParser.ts`:
- Export `useScreenplayParser(sceneId: string)` hook
- Fetches scene content (TipTap JSON) from API or store
- Fetches character voice assignments (character records with voice_id field)
- Fetches project narrator_voice_id (add to project record if missing -- use a sensible default or prompt user)
- Calls parseScreenplayToScriptLines and detectUnassignedVoices
- Returns `{ scriptLines: ScriptLine[], unassignedCharacters: string[], isLoading, refetch }`

Create `src/app/features/voice/hooks/useVoiceCloning.ts`:
- Export `useVoiceCloning()` hook
- State: status ('idle' | 'uploading' | 'cloning' | 'done' | 'error'), error string
- `cloneFromAudio(audioFile: File, voiceName: string)`: upload audio to Supabase Storage, then POST to /api/ai/audio/clone-voice with the URL, returns voice_id
- NOTE: YouTube cloning is deferred -- /api/datasets/audio/extract route does not exist. The hook exposes a placeholder `cloneFromYouTube` that returns an error message directing user to upload audio directly. YouTube extraction can be added in a follow-up when the extraction route is built.
- Returns `{ cloneFromAudio, cloneFromYouTube, status, error }`

Update `src/app/features/voice/hooks/useNarrationBatch.ts`:
- Wire the existing hook to call `/api/ai/audio/tts` (the route from Plan 02)
- Ensure it sends: `{ text: line.text, voice_id: line.voiceId, voice_settings, project_id }`
- On success, update line status to 'generated' and store audioUrl
- Support generating 2-3 takes per line: add `takesCount` option (default 1), loop per line to generate multiple takes
- Store takes in ScriptLine.takes array as ScriptLineTake objects
- Track progress: { current: number, total: number, currentCharacter: string }

Update `src/app/features/voice/hooks/useNarrationExport.ts`:
- Wire to use stitchAudioSegments from Plan 02's audioStitcher
- Fetch all selected take audio URLs, convert to ArrayBuffers
- Call computeGaps(lines) to get silence durations
- Call stitchAudioSegments with buffers and gaps
- Upload stitched result to Supabase Storage
- Return the public URL as MP3 download link

Create `src/app/features/voice/hooks/__tests__/useNarrationBatch.test.ts`:
- Mock fetch to /api/ai/audio/tts
- Test: generateAll with 3 lines calls TTS endpoint 3 times with correct voice_ids
- Test: generateAll with takesCount=2 calls TTS 6 times for 3 lines
- Test: progress updates from {current:0,total:3} to {current:3,total:3}
- Test: failed TTS call sets line status to 'error' and continues remaining lines
  </action>
  <verify>
    <automated>npx vitest run src/app/features/voice/hooks/__tests__/useNarrationBatch.test.ts -x</automated>
  </verify>
  <done>All 4 voice hooks wire to real API routes. useScreenplayParser parses TipTap JSON with voice mapping. useVoiceCloning supports direct audio upload (YouTube deferred). useNarrationBatch generates multiple takes with progress tracking. useNarrationExport stitches and exports MP3. Batch hook has behavioral tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VoiceAssignmentChecklist with rendering test</name>
  <files>src/app/features/voice/components/VoiceAssignmentChecklist.tsx, src/app/features/voice/components/__tests__/VoiceAssignmentChecklist.test.tsx</files>
  <behavior>
    - Renders character names from unassignedCharacters prop
    - Shows "Assign Voice" button for each unassigned character
    - Shows "All Assigned" button when unassignedCharacters is empty
    - Calls onAssign with character name when "Assign Voice" clicked
    - Calls onSkip when "Skip" button clicked
  </behavior>
  <action>
Create `src/app/features/voice/components/VoiceAssignmentChecklist.tsx`:
- Props: `{ unassignedCharacters: string[], onAssign: (character: string) => void, onSkip: () => void }`
- Renders a card listing characters without voice assignments
- Each character row has: name, "Assign Voice" button (opens voice picker), status indicator
- "Skip -- Use Narrator" button to proceed with narrator fallback
- "All Assigned -- Generate" button when list is empty or all assigned
- Style: bg-slate-800/80 border-amber-500/50 (warning tone) rounded-lg

Create `src/app/features/voice/components/__tests__/VoiceAssignmentChecklist.test.tsx`:
- Test: renders character names from unassignedCharacters prop
- Test: renders "Assign Voice" button for each character
- Test: when unassignedCharacters is empty, shows "All Assigned" generate button
- Test: clicking "Assign Voice" calls onAssign with correct character name
- Test: clicking "Skip" calls onSkip callback
  </action>
  <verify>
    <automated>npx vitest run src/app/features/voice/components/__tests__/VoiceAssignmentChecklist.test.tsx -x</automated>
  </verify>
  <done>VoiceAssignmentChecklist renders pre-flight list, fires callbacks correctly. Behavioral rendering tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: NarrationPipeline wiring and MCP/composition integration</name>
  <files>src/app/features/voice/components/NarrationPipeline.tsx, src/mcp-server/tools/voice.ts, src/workspace/config/workflowHints.ts</files>
  <action>
Update `src/app/features/voice/components/NarrationPipeline.tsx`:
- Import and use useScreenplayParser hook to parse scene screenplay into ScriptLines
- Show VoiceAssignmentChecklist if unassignedCharacters.length > 0 before allowing generation
- Wire "Generate All" button to useNarrationBatch.generateAll with takesCount=2 (per user decision: 2-3 takes)
- Wire TakesGallery/TakesModal to display takes for each line and allow audition/selection
- Wire "Export MP3" button to useNarrationExport to stitch selected takes into final MP3
- Show progress during generation (line X of Y, current character name)
- Add voice cloning section: button to open clone dialog, uses useVoiceCloning hook (audio upload only; YouTube clone shows "coming soon" tooltip)
- Preserve existing component interface -- additive changes only

Create or update `src/mcp-server/tools/voice.ts`:
- Add MCP tools following the trio pattern from Phase 9:
  - `generate_scene_narration`: accepts scene_id, generates narration for all screenplay lines (calls TTS route per line). Returns script_lines with audio URLs.
  - `check_voice_assignments`: accepts scene_id, returns list of characters in scene and their voice assignment status
  - `list_voices`: returns available ElevenLabs voices (preset + cloned)
  - `assign_character_voice`: accepts character_id and voice_id, updates character record
- Register tools in the MCP server tool list

Update `src/workspace/config/workflowHints.ts`:
- Add voice/narration workflow hints:
  - When user is in SceneEditor with screenplay format -> suggest "Generate Narration" action linking to narration panel
  - When user opens voice panels -> suggest voice assignment if characters lack voices
  - Pattern: `{ trigger: 'panel_open', panelType: 'narration', suggestions: [...] }`
- Add narration composition patterns to TOOL_PANEL_HINTS:
  - "narrate this scene" -> compose narration + script-dialog panels in split layout
  - "assign voices" -> compose voice-casting + voice-manager panels
  - "record audio drama" -> compose narration + voice-performance + script-dialog in triptych
  </action>
  <verify>
    <automated>npx tsc --noEmit src/app/features/voice/components/NarrationPipeline.tsx src/mcp-server/tools/voice.ts 2>&1 | head -20</automated>
  </verify>
  <done>NarrationPipeline wired to real TTS with screenplay parsing, voice assignment checklist, multi-take generation, and MP3 export. MCP voice tools registered (generate, check, list, assign). Workflow hints added for voice/narration composition. Complete script-to-performance pipeline works end to end.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/app/features/voice/__tests__/ src/app/features/voice/hooks/__tests__/ src/app/features/voice/components/__tests__/ -x` -- all voice tests pass
- `npx tsc --noEmit` -- all modified files compile
- `npm run build:mcp` -- MCP server builds with new voice tools
- NarrationPipeline shows: parse screenplay -> check assignments -> generate takes -> audition -> export MP3
</verification>

<success_criteria>
- Screenplay auto-detection: character cues map to assigned voices, action lines to narrator
- Pre-flight checklist warns about unassigned character voices
- 2 takes generated per segment for audition
- MP3 export stitches selected takes with appropriate silence gaps
- Voice cloning from audio upload available (YouTube extraction deferred to follow-up)
- MCP tools: generate_scene_narration, check_voice_assignments, list_voices, assign_character_voice
- Workflow hints suggest narration panels contextually
- Behavioral tests pass for useNarrationBatch and VoiceAssignmentChecklist
</success_criteria>

<output>
After completion, create `.planning/phases/10-writing-tools-and-voice/10-04-SUMMARY.md`
</output>
