---
phase: 03-jinn-state-and-streaming
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/dzin/core/src/state/types.ts
  - packages/dzin/core/src/state/engine.ts
  - packages/dzin/core/src/state/patches.ts
  - packages/dzin/core/src/state/undo.ts
  - packages/dzin/core/src/state/snapshot.ts
  - packages/dzin/core/src/state/__tests__/engine.test.ts
  - packages/dzin/core/src/state/__tests__/patches.test.ts
  - packages/dzin/core/src/state/__tests__/undo.test.ts
  - packages/dzin/core/src/state/__tests__/snapshot.test.ts
  - packages/dzin/core/package.json
autonomous: true
requirements:
  - JCORE-04
  - JCORE-07

must_haves:
  truths:
    - "LLM-originated JSON Patch operations are applied to workspace state and the state reflects the changes"
    - "User-originated changes are captured as JSON Patch operations via compare() diffing"
    - "Every patch operation carries an origin tag (llm or user)"
    - "Ctrl+Z undoes the entire LLM action as one group, not individual sub-operations"
    - "Undo stack is bounded at 20 entries, oldest entries are evicted"
    - "New dispatch clears the redo stack"
    - "Redo re-applies the undone group faithfully"
    - "Snapshot-on-demand produces a JSON-serializable representation of current workspace state"
  artifacts:
    - path: "packages/dzin/core/src/state/types.ts"
      provides: "WorkspaceState, PanelInstance, StreamingState, TaggedOperation, PatchGroup, PatchOrigin types"
      exports: ["WorkspaceState", "PanelInstance", "StreamingState", "TaggedOperation", "PatchGroup", "PatchOrigin"]
    - path: "packages/dzin/core/src/state/engine.ts"
      provides: "createStateEngine factory function"
      exports: ["createStateEngine", "StateEngine"]
    - path: "packages/dzin/core/src/state/patches.ts"
      provides: "Patch creation, origin tagging, user change capture"
      exports: ["captureUserChange", "createTaggedPatch"]
    - path: "packages/dzin/core/src/state/undo.ts"
      provides: "UndoStack with bounded history and group-aware navigation"
      exports: ["createUndoStack", "UndoStack"]
    - path: "packages/dzin/core/src/state/snapshot.ts"
      provides: "Snapshot serialization for LLM context"
      exports: ["serializeSnapshot"]
  key_links:
    - from: "packages/dzin/core/src/state/engine.ts"
      to: "fast-json-patch"
      via: "applyPatch and compare imports"
      pattern: "import.*from 'fast-json-patch'"
    - from: "packages/dzin/core/src/state/engine.ts"
      to: "packages/dzin/core/src/state/undo.ts"
      via: "UndoStack dependency injection"
      pattern: "createUndoStack"
    - from: "packages/dzin/core/src/state/patches.ts"
      to: "fast-json-patch"
      via: "compare() for user change diffing"
      pattern: "compare\\("
---

<objective>
State engine core with JSON Patch synchronization, undo/redo, and snapshot

Purpose: Build the foundational state engine that all workspace state flows through -- both LLM-initiated patches and user-initiated changes. This is the backbone of bidirectional state sync (JCORE-04) and the undo/redo system (JCORE-07).

Output: Pure TypeScript state engine within @dzin/core that applies RFC 6902 JSON Patch operations, tracks origin (llm/user), manages a bounded undo/redo stack with grouped LLM changes, and produces serializable snapshots for LLM context.
</objective>

<execution_context>
@C:/Users/kazda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-jinn-state-and-streaming/03-RESEARCH.md

<interfaces>
<!-- Key types from @dzin/core that the state engine references -->

From packages/dzin/core/src/types/panel.ts:
```typescript
export type PanelDensity = 'micro' | 'compact' | 'full';
export type PanelRole = 'primary' | 'secondary' | 'tertiary' | 'sidebar';
export interface PanelDataSlice {
  entityId?: string;
  filter?: string;
  view?: string;
  highlight?: string[];
  sort?: string;
}
```

From packages/dzin/core/src/layout/types.ts:
```typescript
export type LayoutTemplateId = 'single' | 'split-2' | 'split-3' | 'grid-4' | 'primary-sidebar' | 'triptych' | 'studio' | 'stack';
export interface ResolvedLayout {
  template: LayoutTemplateId;
  gridTemplateRows: string;
  gridTemplateColumns: string;
  assignments: SlotAssignment[];
}
```

fast-json-patch API (to be installed):
```typescript
import { applyPatch, compare, type Operation } from 'fast-json-patch';
// applyPatch(document, patches, validateOperation?, mutateDocument?) => { newDocument }
// compare(obj1, obj2) => Operation[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: State types, engine core, and patch utilities</name>
  <files>
    packages/dzin/core/package.json,
    packages/dzin/core/src/state/types.ts,
    packages/dzin/core/src/state/engine.ts,
    packages/dzin/core/src/state/patches.ts,
    packages/dzin/core/src/state/snapshot.ts,
    packages/dzin/core/src/state/__tests__/engine.test.ts,
    packages/dzin/core/src/state/__tests__/patches.test.ts,
    packages/dzin/core/src/state/__tests__/snapshot.test.ts
  </files>
  <behavior>
    - Test: createStateEngine initializes with provided initial state
    - Test: getState() returns current state (readonly)
    - Test: dispatch() applies JSON Patch operations to state (add panel, replace layout template, remove panel)
    - Test: dispatch() with origin='llm' tags all patches with origin
    - Test: dispatch() with origin='user' tags all patches with origin
    - Test: dispatch() notifies subscribers with new state and patch group
    - Test: dispatch() generates inverse patches for undo
    - Test: subscribe() returns unsubscribe function that stops notifications
    - Test: captureUserChange() diffs prev/next state and dispatches patches
    - Test: captureUserChange() with no diff produces no dispatch
    - Test: serializeSnapshot() produces JSON string of current state
    - Test: serializeSnapshot() output is parseable back to WorkspaceState
    - Test: engine internal methods _applyWithoutUndo and _recordUndoGroup exist for StreamController integration
  </behavior>
  <action>
    1. Install fast-json-patch: `npm install fast-json-patch` in packages/dzin/core/
    2. Create `packages/dzin/core/src/state/types.ts` with:
       - `PatchOrigin = 'llm' | 'user'`
       - `TaggedOperation extends Operation` with `origin: PatchOrigin`
       - `PatchGroup` with id, patches (TaggedOperation[]), inversePatches (Operation[]), origin, description, timestamp
       - `WorkspaceState` with layout ({ template: LayoutTemplateId, gridTemplateRows: string, gridTemplateColumns: string }), panels (PanelInstance[]), streaming (StreamingState | null)
       - `PanelInstance` with id, type, slotIndex, density, dataSlice?, role, uiState (Record<string, unknown>)
       - `StreamingState` with active, revealedPanelIds (string[]), streamingText (Record<string, string>)
       - `StateEngine<T>` interface with getState, getSnapshot, dispatch, undo, redo, canUndo, canRedo, getHistory, subscribe, _applyWithoutUndo, _recordUndoGroup
    3. Create `packages/dzin/core/src/state/engine.ts` with `createStateEngine<T>(initialState)`:
       - Uses structuredClone for immutable state
       - applyPatch from fast-json-patch with validate=true, mutateDocument=false
       - compare() to generate inverse patches (compare new->old for reverse)
       - Delegates undo stack management to the UndoStack (created in Task 2)
       - Exposes _applyWithoutUndo(newDoc) and _recordUndoGroup(patches, inversePatches, origin, desc) for StreamController
    4. Create `packages/dzin/core/src/state/patches.ts` with:
       - `createTaggedPatch(op, origin)` -- tags a single Operation with origin
       - `captureUserChange(engine, mutator, description)` -- clones state, applies mutator, diffs, dispatches
    5. Create `packages/dzin/core/src/state/snapshot.ts` with:
       - `serializeSnapshot(engine)` -- JSON.stringify of current state
    6. Write tests RED first, then implement GREEN, then REFACTOR.

    IMPORTANT: For the engine, temporarily stub undo/redo methods (return null/false) since the UndoStack is built in Task 2. The engine should accept an optional UndoStack parameter that defaults to a no-op stub. Task 2 will provide the real implementation.

    Use `crypto.randomUUID()` for PatchGroup IDs. Use `structuredClone()` for deep copies.
  </action>
  <verify>
    <automated>cd packages/dzin/core && npx vitest run src/state/__tests__/engine.test.ts src/state/__tests__/patches.test.ts src/state/__tests__/snapshot.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - createStateEngine creates engine with initial state
    - dispatch() applies patches, generates inverse patches, notifies subscribers, tags origin
    - captureUserChange() diffs and dispatches user changes
    - serializeSnapshot() produces parseable JSON
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Undo/redo stack with grouped LLM changes</name>
  <files>
    packages/dzin/core/src/state/undo.ts,
    packages/dzin/core/src/state/engine.ts,
    packages/dzin/core/src/state/__tests__/undo.test.ts
  </files>
  <behavior>
    - Test: createUndoStack initializes empty stacks
    - Test: push() adds a PatchGroup to undo stack
    - Test: push() clears the redo stack
    - Test: undo() pops from undo stack, pushes to redo stack, returns group
    - Test: undo() returns null when stack is empty
    - Test: redo() pops from redo stack, pushes to undo stack, returns group
    - Test: redo() returns null when stack is empty
    - Test: canUndo/canRedo return correct booleans
    - Test: bounded history -- pushing 21st entry evicts the oldest (entry 0)
    - Test: bounded history -- MAX_DEPTH=20, stack never exceeds 20
    - Test: getHistory() returns copy of undo stack (not reference)
    - Test: integration with engine -- dispatch creates undo group, undo() reverts state, redo() re-applies
    - Test: LLM batch undo -- 4 patches dispatched as one group, single undo reverts all 4
    - Test: interleaved user + LLM groups -- undo walks back through both in order
    - Test: undo after 20+ dispatches only has 20 entries (oldest evicted)
  </behavior>
  <action>
    1. Create `packages/dzin/core/src/state/undo.ts` with `createUndoStack(maxDepth = 20)`:
       - `push(group: PatchGroup)` -- adds to undo stack, evicts oldest if > maxDepth, clears redo
       - `undo()` -- pops from undo, pushes to redo, returns group (or null)
       - `redo()` -- pops from redo, pushes to undo, returns group (or null)
       - `canUndo()` / `canRedo()` -- boolean checks
       - `getHistory()` -- returns [...undoStack] copy
       - `clear()` -- empties both stacks
    2. Update `packages/dzin/core/src/state/engine.ts`:
       - Wire createUndoStack into createStateEngine
       - undo() applies inverse patches from the group, notifies subscribers
       - redo() applies forward patches from the group, notifies subscribers
       - Remove the temporary stubs from Task 1
    3. Write tests RED first, then implement GREEN, then REFACTOR.

    Per user decision: "Hybrid grouped undo -- Ctrl+Z reverts the entire LLM action as one step." This is achieved because dispatch() creates one PatchGroup per call, and LLM batches all sub-operations into a single dispatch().

    Per user decision: "Unified undo stack -- one stack for both LLM-initiated and user-initiated changes."
  </action>
  <verify>
    <automated>cd packages/dzin/core && npx vitest run src/state/__tests__/undo.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - UndoStack enforces 20-entry max depth
    - Undo reverts entire LLM batch as one step
    - Redo re-applies undone group
    - New dispatch clears redo stack
    - Engine undo/redo modifies state correctly and notifies subscribers
    - All tests pass
  </done>
</task>

</tasks>

<verification>
Run all state tests together:
```bash
cd packages/dzin/core && npx vitest run src/state/__tests__/ --reporter=verbose
```
Confirm all pass. Also run the full dzin suite to verify no regressions:
```bash
npx vitest run --project dzin --reporter=verbose
```
</verification>

<success_criteria>
- fast-json-patch installed in @dzin/core
- State engine applies JSON Patch operations immutably
- Origin tagging works for both llm and user sources
- Undo stack bounded at 20 with grouped LLM changes
- Redo works correctly, cleared on new dispatch
- Snapshot serialization produces valid JSON
- All tests pass, no regressions in existing Phase 1-2 tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-jinn-state-and-streaming/03-01-SUMMARY.md`
</output>
